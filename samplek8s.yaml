apiVersion: v1
kind: Pod
metadata:
  name: test-bad-image
  namespace: default
  labels:
    app: test-bad-image
spec:
  containers:
  - name: nginx
    image: nginx:nonexistent-version   # ❌ Invalid image tag to trigger ImagePullError
    ports:
    - containerPort: 80
  restartPolicy: Always